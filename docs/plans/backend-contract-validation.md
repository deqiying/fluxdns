# 后端契约验证开发计划

> 文档状态：有效
>
> 计划状态：实施中
>
> 适用范围：已验收后端契约的组合测试、隔离环境验收、容量恢复与长期负载验证
>
> 代码基线：`749e1ec2f8e260ff7f43a598e27683d1a377af16`
>
> 最后核对：2026-09-05（UTC），用户已确认评审通过；从 `f65fb3f8bd68e1a40ca041d9a380859b44a3da0c` 的干净工作树开始实施
>
> 剩余清单核对：2026-09-06，以 `aaa400c37b433a0c3f3554105686e552cb4e4dc2` 核对源码、驱动与证据边界；本次仅细化文档，未补跑环境实验

## 执行状态

本计划**尚未整体完成**。本轮已交付本机验证增量、显式连接驱动和可重复证据入口；三段状态按下表保留，不能用默认全量回归替代每包剩余矩阵。

| 工作包 | 测试/脚本实现 | 实际执行与断言 | 剩余范围/门槛 |
| --- | --- | --- | --- |
| V0 | 已建立测试编号、共享同步点及记录命令/指纹/失败的 runner | Windows 工具链、loopback、SQLite 可用；无 Git 既有脏改动 | 后续目标环境尚未在本任务中指定/核验，输入要求见[共用门槛](#101-共用启动与证据门槛) |
| V1 | 分步发布、双资源 CAS、两类时钟、真实 service refresh/reload/rebind、当前/历史 finalizer 及三类后台 owner panic | 本机编号矩阵通过；复现并最小修复 reload activation 等锁超预算；失败候选不替换活动 runtime | 与真实远程刷新、Unix 信号的组合仍依赖 V7/V8；内部回收不等同于统一自动恢复 |
| V2 | nested group 中 primary 提交/丢弃 × 同代/换代 × late 前停机；独立 follower 取消、负答案后取消、publisher 拒收与 finalizer 终态 | 本机编号矩阵通过；复现并最小修复未 poll abort 的 finalizer active 泄漏 | 不宣称穷举所有因素排列；真实满载/介质故障导致的持续积压与联合恢复仍由 V5/V9 验收 |
| V3 | 真 TCP stream、HTTP/HTTPS × SOCKS 目标/Host/SNI/TLS 顺序、坏证书/body、TLS/头/body 与三段 SOCKS 共享预算；复用 bootstrap/拒绝 suite | 新增本地矩阵通过；不含远程服务 | [网络分支执行单](#102-v3-剩余网络分支)：挂起拨号、跨 OS 终态、body 分类决策分别跟踪 |
| V4 | v1–v5 升级重开/失败回滚、UTC late event、事件保护、真实 SQL 三阶段 × 正常/超时停机、并发 flush 等锁 | 本机 SQLite/内存矩阵通过；复现并最小修复 stats flush 等锁超预算，显式恢复不重复统计 | 介质故障、持续容量/恢复和长时间联合负载仍由 V5/V9/V10 验收；不承诺强制抢占 SQL |
| V5 | 复用既有生产 adapter，未新增真实介质故障驱动 | 未执行，未验收 | [介质故障执行单](#103-v5-真实介质故障)：disk-full、权限、I/O 各自确认故障目标、作用阶段及恢复方法 |
| V6 | V6-C01 显式容量驱动已实现；复用本地 TLS/PROXY/forwarded suite | TCP/plain DoH 1,023/1,024/超额等待、单槽释放、停机端口回收已实测 | [连接与代理执行单](#104-v6-连接恢复与真实代理)：区分本机可补驱动、external 入站代理和真实出站 SOCKS 环境 |
| V7 | 复用已有 loopback 条件资源夹具；未新增真实远程专用驱动 | 既有 loopback suite 本机回归；远程未执行 | [远程资源执行单](#105-v7-可控远程资源)：端点控制能力、条件请求、pair/代际和刷新/reload 逐项验收 |
| V8 | 生产 Unix 信号分支未改；真实子进程信号驱动待目标环境确定 | 当前仅 Windows；未执行 Unix 验收 | [Unix 执行单](#106-v8-unix-进程信号)：真实 PID、首/第二信号时序、超时分类与退出后清理 |
| V9 | 新增三轮 SQLite trigger 故障/恢复与 pending 事件数保护的本地子集 | 局部正确性通过，不是持续负载验收 | [持续恢复执行单](#107-v9-持续积压与联合恢复)：阈值前恢复、触限退出、Cache 和连接/owner 分开记录 |
| V10 | 本计划保留设计参数表；未新增长期负载/性能对照驱动 | 未执行，未验收 | [长期负载执行单](#108-v10-长期负载与性能验收)：待冻结参数、工作负载、资源趋势和通过/停止阈值 |

具体用例、三处生产修复和证据分别沉淀在[生命周期](../implementation/backend/lifecycle.md#契约验证补充)、[DNS 管线](../implementation/backend/dns-pipeline.md#契约验证补充)及[后台服务](../implementation/backend/background-services.md#契约验证运行入口)。本轮 Local runner 的 27 项新增默认测试重复 3 次均通过，完整回归 669 通过、0 失败、3 忽略；V6-C01 另行显式重复 3 次通过，两组源码指纹一致。未达到第 9 节退出条件，因此保留本计划与索引，不进入“已完成”或删除。

上述表格仅作导航；剩余事项的具体输入、驱动缺口、执行步骤与关闭条件统一维护在[第 10 节](#10-剩余验收执行清单)。不能把“驱动待实现”“环境未指定/核验”和“策略待决策”合并为一个“缺环境”；本机可完成的驱动工作不因其他环境缺失而暂停。

准备开发和测试环境时，先按[第 11 节依赖清单](#11-测试开发依赖环境清单)确认必需组件、实际能力、权限、就绪检查与阻塞范围；仅提供主机地址、代理 URL 或空目录不代表依赖已经就绪。

## 1. 目标与现状

D1-D9 已由用户确认验收，正式契约保存在[后端设计](../architecture/backend/overview.md)及其模块文档中；业务时间整数迁移也已提交。本计划独立承接尚缺的验证证据，不重新实施已完成的功能，不因后续环境测试尚未运行而重新打开 D1-D9 的设计选择。

现状依据：

- [DNS 管线](../implementation/backend/dns-pipeline.md)记录生产 Policy、Upstream、Cache 与 late finalizer 的接线；已有单场景测试不能证明全部跨 revision、取消和 shutdown 组合。
- [生命周期](../implementation/backend/lifecycle.md)与[后台服务](../implementation/backend/background-services.md)记录 owner、共享 deadline、统计优先和既有本地验证；本地 SQLite 锁、trigger 与错误注入不能替代真实故障介质。
- [SQLite Storage](../../backend/src/storage/sqlite.rs)已有 v1 升级、v5→v6 数据/ledger/自增序列保留、异常时间回滚与重开用例；剩余任务是补齐升级起点和运行组合，不是新增一次时间迁移。
- [FakeClock](../../backend/src/ports/testing.rs)只推进自身 Clock 状态；[Supervisor](../../backend/src/runtime/supervisor.rs)仍有 Tokio retry timer，不能用一类时钟测试替代全部时间行为。
- [Service](../../backend/src/service.rs)已有真实信号分支及 TCP/DoH session 数量限制；边界函数测试不能证明真实连接满载、释放与恢复。

交付目标是建立可重复的测试、运行入口和证据。默认只补测试及必要夹具；仅在复现既定契约缺陷后做最小生产修复。测试实现、实际运行通过、目标环境验收是三个不同状态，必须分别记录。

## 2. 保留契约与非目标

1. 配置含义、默认值和路径解析不变；缓存持久化仍按编码内容与 framing 计费、按插入时间淘汰，不改为物理硬配额或 LRU。依据：[配置参考](../implementation/configuration.md)、[Cache](../architecture/backend/modules/cache.md)。
2. 保留单次无等待完成事件移交和后台缓存/统计/指标/详情处理，不把 SQLite I/O 或同步聚合移回响应主链；保留 bootstrap 地址缓存与后台采样。依据：[后台服务](../implementation/backend/background-services.md)。
3. 正常 terminal response 包括 NXDOMAIN，阻止启动 fallback；允许等待已启动同阶段成员的更好答案，Positive 首响应后保留生产 late-result 收集；负答案不额外续期，in-flight 保持 primary lease 口径。依据：[Upstream](../architecture/backend/modules/upstream.md)。
4. 停机尽快取消在途请求，不保证所有已读请求完成写回；启动/停机共享总 deadline。已进入 OS/SQLite 的操作不承诺被强制中断，统计优先也不意味着抢占当前详情事务。依据：[Runtime](../architecture/backend/modules/runtime.md)、[Storage](../architecture/backend/modules/storage.md)。
5. 业务绝对时间保留 schema v6 的非负 `INTEGER` UTC Unix 毫秒；缓存独立纳秒索引、耗时精度和 UTC 日桶各自不变，不补造历史字段，不新增 down migration。依据：[业务时间存储](../implementation/backend/background-services.md#业务时间存储)。
6. 条件请求只复用已校验且身份/代际匹配的本地内容；304 重试共享原预算。保留现有 panic 脱敏、错误分级和升级策略，不把 owner panic 自动恢复当作本计划目标。

不新增配置项、DoT/DoQ、主动健康检查、通用 admission limiter、完整逐 attempt span、持久审计 spool 或新的生产测试注册层。不顺手修改前端/API，不预先引入测试框架、系统代理、CI 服务或工具依赖。改变兼容性、资源预算或失败策略的修复须另行确认。

## 3. 拆分与推荐顺序

推荐按表顺序推进；“前置”是对应证据依赖，不要求无关工作包全部完成。每包可独立交付、评审和验收，共享夹具由 V0/V1 维护，不为每包复制一套环境或并发控制。

| 阶段 | 工作包 | 交付主题 | 前置 |
| --- | --- | --- | --- |
| 准备 | V0 | 用例清单、状态与证据约定、环境盘点 | 本计划获批 |
| A：本地确定性 | V1 | Runtime 并发与时间控制 | V0 |
| A：本地确定性 | V2 | late-window / owner 组合 | V1 的时序与 owner 夹具 |
| A：本地确定性 | V3 | Adapter 本地 conformance | V0；涉及 reload 的场景复用 V1 |
| A：本地确定性 | V4 | Storage 升级矩阵、统计与停机边界 | V0；并发场景复用 V1 |
| B：隔离环境 | V5 | Cache/Storage 真实数据库故障与恢复 | V4、获授权隔离介质 |
| B：隔离环境 | V6 | DoH/TLS 信任链、连接限额与恢复 | V3、可控 TLS/代理环境 |
| B：隔离环境 | V7 | 真实远程资源条件请求 | V3、可控远程端点 |
| B：隔离环境 | V8 | Unix 真实进程信号与退出 | V1/V2/V4 的生命周期边界、Unix 环境 |
| C：持续运行 | V9 | 积压保护、容量与连接的持续恢复 | V4/V5；连接部分依赖 V6 |
| C：持续运行 | V10 | 稳态、故障周期与长期性能对照 | V9、所测链路相关 A/B 项、冻结的负载与预算 |

原七类剩余范围完整映射如下，拆分不等于减少验收：

| 原范围 | 新工作包 |
| --- | --- |
| late-window / owner | V2 |
| 真实数据库故障 | V5 |
| Adapter conformance | V3，本地 TLS 信任链延伸至 V6，真实资源请求延伸至 V7 |
| Runtime 并发与时间控制 | V1 |
| Storage migration 与压力 | V4、V9 |
| DoH/TLS 安全与限额 | V6、V9 |
| Unix 信号与长期负载 | V8、V10 |

环境不足仅阻塞相应运行验收及其依赖项。例如，V5 缺隔离介质不阻塞 V3/V4；V8 缺 Unix 不阻塞本机确定性测试。V10 不能将缺少前置证据的场景算作已验收；缩小目标环境或场景范围须经明确确认并保留限制。

## 4. V0：验收基线与执行约定

先复用各模块现有测试和源码内嵌夹具，建立编号用例清单，不先建设通用测试框架。用例定义应随实际测试代码维护；本计划只管理剩余范围，正式结果沉淀到对应 implementation，原始输出留在本地专用目录。

每个用例至少明确：

- 既定契约、对应工作包、生产入口、前置状态、故障/竞争发生点与预期终态。
- 覆盖因素、支持/不支持的组合及其依据。每个列明因素和失败阶段都要覆盖；高风险交互必须有定向三因素或以上组合，不能仅用两两组合宣称“完整矩阵”。
- 触发和同步方式、操作 deadline、测试 watchdog、必要的重复次数与清理检查；不以调大 sleep 掩盖竞争失败。
- 三段进度：测试/脚本是否实现、适用环境是否执行、断言是否通过；失败和未执行分别记录原因，不能通过忽略测试消除缺口。
- 源码 commit、工具链、命令、配置与脱敏输入、时序/种子、结果、遗留问题，以及数据库/进程/task/句柄的清理结果。

Clock 与调度控制按实际等待点选择：已有 Clock 注入继续复用；消息、barrier 或通知控制发布/取消顺序；Tokio timer 的暂停/推进须先核对当前 feature 和真实 I/O 限制。必要测试开关仅位于测试边界，不增加生产配置或第二套 Runtime。读取真实数据库、跨进程信号和真实 I/O 故障必须保留对应运行证据。

完成条件：原七类范围均有可执行用例分解和归属；列清当前本机、Unix、远程、故障介质与负载环境是否可用。性能阈值尚未确定只阻塞阶段 C，不阻塞阶段 A。

## 5. 阶段 A：本地确定性测试

### V1：Runtime 并发与时间控制

源码入口：[coordinator.rs](../../backend/src/runtime/coordinator.rs)、[prepared.rs](../../backend/src/runtime/prepared.rs)、[supervisor.rs](../../backend/src/runtime/supervisor.rs)、[snapshot.rs](../../backend/src/runtime/snapshot.rs)、[ports/testing.rs](../../backend/src/ports/testing.rs)。

1. 枚举资源 Policy 发布、metadata 发布、revision CAS、rebind、drain、retry 和内部 owner 回收的线性化/等待点，补最少的测试同步能力。
2. 显式覆盖 Policy 成功后 metadata 尚未发布、旧刷新迟到、新 candidate 发布、两个不同资源竞争、失败候选不替换活动实例；验证分步发布的现有语义，不擅自改成跨对象原子事务。
3. 覆盖 reload 与刷新竞争、旧 task 结束/失败/取消及当前/历史 owner panic；分别验证 Supervisor 直接任务与内部 worker 的归属、升级及回收。
4. 将 retry、请求 deadline 和 shutdown budget 分别映射到实际时钟；同一 deadline 不因重试或阶段切换重置。

完成条件：无需碰运气的 sleep 即可触发目标竞争；旧结果不能覆盖不匹配的新代际，无永久 guard/lease/task 遗留；panic 仍脱敏、正确归因并按现有策略处理。缺陷先有复现用例，再做最小修复。

### V2：late-window / owner 组合

源码入口：[dns/policy.rs](../../backend/src/dns/policy.rs)、[upstream/executor.rs](../../backend/src/upstream/executor.rs)、[service.rs](../../backend/src/service.rs)及其现有 finalizer 用例。

1. 以当前/历史 revision、producer leader/follower、首响应/晚响应、Positive/正常负答案/失败、取消时点、sink 可用/满/关闭、运行/shutdown 为因素建立矩阵；加入 nested group。
2. 必测交互包括：负答案先到且同阶段 Positive 晚到；已有 NXDOMAIN 后另一 primary 超时而不启动 fallback；Positive 首响应后 reload 再收到 late result；sink 满时 producer 结束并唤醒 waiter；跨 revision 与 shutdown 同时发生。
3. 分别断言客户端响应只确定一次、late candidate 不改写已响应内容、缓存写回匹配 key/quality/producer 约束、primary lease 口径不变。
4. 覆盖成功、拒收、取消、失败和 drop 后的 lease/waiter 终态；当前与历史 finalizer 均受 owner 管理，在既有 deadline 下结束或报告未完成。

完成条件：没有悬挂 waiter、重复响应、跨代错误覆盖或脱管 drain；队列满/丢弃有对应计数。不能为通过测试关闭 late-result、延长预算或恢复“响应完成后才允许停机”。

### V3：Adapter 本地 conformance

源码入口：[ports/testing.rs](../../backend/src/ports/testing.rs)、[tokio_outbound.rs](../../backend/src/upstream/tokio_outbound.rs)、[bootstrap.rs](../../backend/src/upstream/bootstrap.rs)、[socks5.rs](../../backend/src/upstream/socks5.rs)、[reqwest_http.rs](../../backend/src/upstream/reqwest_http.rs)、[doh.rs](../../backend/src/upstream/doh.rs)、[system_socket.rs](../../backend/src/runtime/system_socket.rs)。

1. 先核对现有支持关系，分别列出入站 UDP/TCP/DoH 与上游 HTTP/HTTPS、direct、bootstrap、`connect_ip`、SOCKS5/SOCKS5H 的适用组合；不支持的组合验证既有拒绝行为，不实现新协议。
2. 复用真实 loopback server/dialer，按每类 adapter 可表达的契约复用断言；记录实际拨号地址、代理 CONNECT 地址类型、HTTP Host、TLS SNI 与校验证书身份，不能只检查配置值。
3. 覆盖本地/代理侧解析、显式 IP 与域名身份分离、TLS/PROXY 顺序、bootstrap 超时、拨号/握手/读取取消、EOF 和坏响应；这些阶段共享剩余预算。
4. 在测试中调用适用 conformance 断言，不增加运行期注册门禁。既有测试足够的场景只复用和记录，不重复维护两套 suite。

完成条件：支持矩阵每项有真实 adapter 证据或明确的拒绝断言；域名/IP/Host/SNI 不混用，错误分类与取消满足 port 契约。此包不宣称远程服务、外部 TLS 信任链或全部生产网络已验收。

### V4：Storage 升级、统计与停机边界

源码入口：[migrations](../../backend/migrations)、[sqlite.rs](../../backend/src/storage/sqlite.rs)、[backend_contract_tests.rs](../../backend/src/storage/backend_contract_tests.rs)、[statistics.rs](../../backend/src/storage/statistics.rs)、[stats.rs](../../backend/src/storage/stats.rs)、[service.rs](../../backend/src/storage/service.rs)。

1. 用历史 migration 构造新库及 v1-v5 各起点的可识别数据，升级至 v6 后重开；覆盖空库、含数据、历史 nullable 字段、删除后自增高水位、规范时间、异常时间与不支持的较新版本。旧 migration 不重写。
2. 对照全部目标字段、统计总数/维度、ledger hash/序号、详情 ID 和实际 INTEGER 时间类型；分别断言每步事务失败保留该步之前状态，不能误称整条升级链一次性回滚。
3. 用事件时间覆盖 UTC 午夜前后、乱序和 late event、epoch swap、同批重试；保证按事件所属日聚合且已提交批次不重复计数，不补造历史事实。
4. 覆盖详情批写、软/硬容量边界、清理顺序、pending/gap；在 SQL 未开始、已开始、完成待回收与停机截止接近时验证统计优先和总预算。

完成条件：各升级起点与异常路径有独立断言；统计幂等、时间单位、详情投影和主链耗时精度保持既定契约。SQL 已执行时的 deadline 耗尽必须真实报告，不以“可抢占事务”作为通过条件。长时容量与故障恢复另由 V5/V9 证明。

## 6. 阶段 B：隔离环境验收

### V5：真实数据库故障与恢复

在 V4 的断言基础上分别运行 Cache 与业务 Storage 的生产 adapter。执行前确认独立可丢弃介质、绝对目标路径、空间上限、恢复操作、watchdog 和授权，禁止填满系统盘、操作共享/生产数据库或故障化个人工作目录。

1. 真实 disk-full、权限失败、I/O 失败分别执行；覆盖启动建库/迁移/写探针和运行批写/提交/checkpoint 等适用阶段，并区分主库与 WAL 的失败位置。
2. 先写可识别基线，再制造故障，保留实际 OS/SQLite 错误、介质状态及失败阶段；只触发 hook、trigger 或 busy lock 不算相应介质故障通过。权限变更必须确认实际 I/O 已失败，不能只以权限命令成功为证据。
3. 故障期间检查旧数据、事务原子性、pending/ledger/gap、DNS 响应和时延、RSS/CPU/句柄；分别断言 Cache best-effort 降级与 Storage pending 保护/失败升级。
4. 恢复介质后验证重试或重启、数据一致性、已提交批次去重与资源释放。未达到保护阈值的恢复和达到阈值后的有限退出分开验证，不要求无限 pending 或零丢失。

完成条件：每种故障都有实际介质证据、恢复或按契约退出结果，数据库逻辑内容与完整性检查通过；受限环境不能复现的故障保持未验收。不得安装额外驱动/代理或扩大介质权限来绕过环境门槛。

### V6：DoH/TLS 信任链与真实连接限额

源码入口：[transport/doh.rs](../../backend/src/transport/doh.rs)、[system_socket.rs](../../backend/src/runtime/system_socket.rs)、[service.rs](../../backend/src/service.rs)。与 V3 共享证书、HTTP 和 PROXY 夹具，新增验证生产 listener 的持续连接行为。

1. 分别验证当前支持的 TLS 模式、受信/不受信 peer、forwarded 多跳链、伪造头、证书身份不匹配和 PROXY/TLS 读取顺序；GET/POST 同时检查 HTTP 与 DNS 层结果。
2. 无效 TLS、截断/畸形 HTTP、慢握手、慢 body 和单连接异常不得破坏其他有效连接；断言关闭范围、错误计数和仍可接收请求的能力。
3. 对单 TCP/DoH listener 实际占用 1,023、1,024 及更多连接，区分内核 backlog、已 accept session 与正在处理请求。验证现有 session 上限处暂停 accept，释放槽位后恢复，而不是把超过上限的 TCP connect 一律要求立即失败。
4. 在满载、释放、重复重连、reload 和 shutdown 期间检查 session task/句柄；不将该限制解释为跨 listener 总配额或 UDP 通用 admission limiter。

完成条件：错误信任链不能伪造有效来源，坏连接隔离有效，满载与释放恢复有真实连接证据。`external` 模式需要现有且获授权的代理环境；没有环境时记录缺口，不临时安装反向代理，不将该模式标为通过。

### V7：真实远程资源条件请求

源码入口：[fetcher.rs](../../backend/src/resource/fetcher.rs)、[remote.rs](../../backend/src/resource/remote.rs)、[prepared.rs](../../backend/src/runtime/prepared.rs)。使用已获准、可控制响应和版本的远程端点，不把任意公共 URL 的偶然 200/304 当作完整验收。

1. 首次 200 从响应取得 validator，后续条件请求分别取得内容不变的 304、内容变化的新 200；对照请求头、响应状态、内容 hash/身份、matcher 与本地内容/manifest pair。
2. 覆盖 pair 损坏/缺失、URL/adapter generation 变化、重复 304、无效新内容与取消后的重试；验证不误复用、不替换有效 snapshot、不重获 deadline。
3. 叠加资源刷新与 reload，复用 V1 的代际断言；故障后验证上次有效内容的保留或按既定启动契约失败。

完成条件：远程记录与本机发布结果能够对照，敏感头和地址不进入共享报告。只有 loopback 或环境无法控制所需响应时，仅记录已验证子集，不关闭远程矩阵。

### V8：Unix 真实进程信号与退出

复用 [Service](../../backend/src/service.rs) 的 `wait_for_termination_signal` 与 shutdown 顺序，在获授权 Unix 环境运行真实 binary，而不是只对 Cancellation 调用做单测。

1. 覆盖 SIGTERM、SIGINT，以及首信号触发 drain 后、grace 尚未结束时收到第二信号；受控延迟保证第二信号确实命中退出分支。
2. 用空闲、DNS 在途、late drain、详情写入和 pending stats 场景核验停止接收、尽快取消、统计/详情顺序及退出结果；区分正常退出、deadline 耗尽和第二信号错误退出。
3. 同时记录退出码、实际 elapsed、阶段报告和资源终态；核验监听端口释放、无遗留子进程、数据库可重开，未完成项不伪装为成功。

完成条件：证据包含 Unix 版本、真实进程 PID、发送信号顺序与时间、退出结果和清理检查；Windows 上同名辅助测试或人工终止按钮不能替代。测试 watchdog 的兜底 kill 单独记为失败/清理动作，不算正常 shutdown。

## 7. 阶段 C：持续恢复与长期负载

### V9：积压、容量与连接恢复

复用 V4/V5 的数据断言和 V6 的连接驱动，持续执行“稳态 → 增压/故障 → 解除 → 恢复/有限退出”。阈值从当前源码/配置读取并记录，不新增测试专用生产预算。

- Storage：组合统计 epoch、跨午夜/late event、批次去重、详情软/硬容量及年龄清理；分别验证 pending 批数/事件数的阈值前恢复和阈值触发后的升级、有限 flush/退出。
- Cache：在插入/淘汰、持久化写入失败、namespace 变化和 reload 中检查编码预算与恢复数据；物理主库/WAL/SHM 只作为观测值，不套用编码预算断言。
- 连接与 owner：重复满载/释放、reload/刷新、late candidate 与 shutdown，检查 session、guard、lease、队列、task 和句柄是否回到预期终态。
- 停机：压力下分别覆盖空闲 SQL、当前 SQL 接近截止、stats pending 和多批详情；报告谁消耗预算以及剩余详情，不要求事务抢占或重置 grace。

完成条件：每种适用场景至少经历评审冻结的多轮周期；无重复入账、无未解释的数据变化，无超出既有保护的持续队列增长。允许的 best-effort 丢弃必须对应可解释计数；达到 fatal 阈值后的有限退出是单独通过路径，不要求仍继续接收 DNS。

### V10：长期负载与性能对照

测试设计先完成，运行门槛后确认。不能从旧 debug 短时 profile 推导发布性能、SLO 或长期内存稳定性。

开始运行前冻结以下参数；任何缺项仅允许称为探索性测量，不能称为性能验收：

| 参数 | 必须记录的内容 |
| --- | --- |
| 版本与构建 | 源码 commit、Rust/依赖基线、release feature、配置指纹；对照版本与候选版本只改变本次目标差异 |
| 环境与隔离 | OS/内核、CPU/内存、文件系统/介质、进程与连接限制、网络位置、其他负载；确认负载发生器不是瓶颈 |
| 输入负载 | 各协议比例、QPS、并发、连接复用、查询集合、命中/未命中/负答案比例、详情开关、上游延迟/失败模式 |
| 时间安排 | 预热、稳态、故障/恢复周期、总时长、采样间隔和重复次数；建议从 10 分钟预热、30 分钟对照、4 小时持续运行及至少 3 次恢复周期起评审，不自动执行该时长 |
| 通过与停止阈值 | DNS 成功率及 p50/p95/p99、CPU、RSS 与增长趋势、句柄/task/队列、磁盘余量、允许退化幅度、恢复窗口和最大停机观察时间 |

1. 同一环境分别跑 Cache 命中、真实上游未命中、负答案、详情开/关以及受控混合流量；每类记录实际完成 QPS，成功、失败与超时分开统计数量和耗时，不将超时伪装为成功延迟，也不能只用成功样本概括整体服务表现。
2. 在持续负载中按固定计划叠加 reload、资源刷新、上游故障和数据库恢复，复用 V9 的正确性断言；不同故障源先单独再组合，确保可以归因。
3. 记录主链/端到端耗时、RSS/CPU、队列/owner/连接数、gap/drop、数据库/缓存物理占用及恢复轨迹；主链耗时不混入异步落库等待，端到端测量说明客户端排队边界。
4. 对照测试必须固定构建模式和输入，多轮结果报告分布与波动，不从单轮百分比给结论。没有业务 SLO 时只给同机对照结论，不声称已达到生产容量。

完成条件：冻结的正确性、资源、性能和恢复阈值全部满足，且没有未解释的持续资源增长；触发安全停止条件则本轮失败或未完成，保留证据。若修复了会影响时序/性能的代码，应重新冻结版本并重跑受影响的对照，不能拼接不同版本的曲线。

## 8. 开发、运行与证据交付

### 最小变更策略

优先在现有模块 `#[cfg(test)]` 和已有 contract suite 中补用例；跨进程/环境驱动复用项目现有能力。只有可复现缺陷才修改其所属生产模块，保留用户配置语义和异步主链。新增 dependency、系统工具、CI 或部署入口不由本计划自动授权。

每包先交付可重复入口及失败断言，再执行适用环境验证；修复同批更新测试与直接受影响文档。测试代码完成但缺环境的包可独立交付，但运行状态仍为未验收，不阻塞其他无依赖包。

### 命令与产物

执行前遵守[环境规范](../rules/environment-usage.md)与[本地测试规范](../rules/local-testing.md)，核对实际工具链。以下是计划执行入口，不是本次已通过记录：

```powershell
cargo fmt --manifest-path backend/Cargo.toml -- --check
cargo check --manifest-path backend/Cargo.toml --locked
cargo test --manifest-path backend/Cargo.toml --locked --quiet -- --test-threads=4
pwsh -File .agents/skills/project-doc-maintenance/scripts/check-docs.ps1
git diff --check
```

先跑受影响模块定向测试，再跑全量回归；含真实故障、外网、长时间负载和 Unix 子进程条件的测试必须显式选择并检查环境，不能进入无条件默认执行路径。实际筛选名称由新增测试确定，不把上面的全量命令当成所有环境验收入口。

运行配置、证书、数据库、日志和原始测量统一放在 `_fluxdns/`；本机测试 TEMP/TMP 指向 `_fluxdns/test-temp`，Unix 临时路径同样按本地规则设置。隔离介质使用独立测试环境中的专用工作目录，并记录实际绝对路径与授权范围。合成夹具与可复现驱动可跟随测试源码提交，不提交个人数据库、凭据或实际远程地址。

### 证据落点

| 工作包 | 正式结果与实现说明 |
| --- | --- |
| V1、V8 | [生命周期](../implementation/backend/lifecycle.md) |
| V2、V3、V6 | [DNS 管线](../implementation/backend/dns-pipeline.md)；生命周期/后台 owner 证据按实际职责链接 |
| V4、V5、V7、V9、V10 | [后台服务](../implementation/backend/background-services.md)；协议负载证据引用 DNS 管线，不重复整份报告 |

结果注明静态、fake/loopback、真实 SQLite、远程、Unix 或负载证据类别。复用旧结果时注明旧基线和未重跑范围。每包保留场景与实际结果、失败原因、环境缺口和复现入口；不要把新增测试数量当作覆盖率。

## 9. 风险、门槛与退出

- 并发测试的假稳定：用同步点控制目标竞争，保留 watchdog；重复测试与不同合法交错补充确定性用例，不用长 sleep 或无限 retry 消除失败。
- 环境/破坏性动作：故障仅限明确授权的可丢弃介质和进程；磁盘满、权限变更、信号和长时负载执行前核验目标、恢复步骤及资源上限，禁止共享/生产目标。
- 验收无限扩张：V0 列清支持组合与风险因素，评审后冻结；新发现的跨契约设计问题单独确认，不把“不穷举所有排列”写成全部组合已通过。
- 时钟和期限误判：测试 watchdog 与业务 deadline 分离，记录真实 SQL/OS 无法抢占的限制；不得为了通过延长业务 budget。
- 观测反向影响主链：测量开销单独核对，不新增同步请求日志或高基数生产指标来获取测试证据；本地原始记录使用合成数据并脱敏。

本计划已获批，按 V0 和 A/B/C 推荐顺序实施。阶段 C 的具体负载/预算、真实故障介质、远程端点与 Unix 环境在对应阶段开始前确认；未确认时记录门槛，不自动安装或扩权。

退出条件：

1. 七类原范围均取得其要求的证据，或用户明确调整某项验收范围；未执行项不能仅因脚本已提交而关闭。
2. 发现的既定契约缺陷修复并通过相关回归；设计范围变化已另行确认，配置和异步主链约束未被隐式改变。
3. 最终事实、运行证据及接受的残余限制沉淀到上表 implementation；改变设计时同步对应 architecture。
4. 删除本计划、索引项及仅为活动验收保留的入站引用，不建立 archive/history 或旧路径跳转副本；历史由 Git 追溯。

## 10. 剩余验收执行清单

本节是 `aaa400c37b433a0c3f3554105686e552cb4e4dc2` 之后的**待办执行单，不是运行结果**。下列 `R` 编号是计划用例编号，尚不代表存在同名测试或命令。每项分别记录驱动状态、环境状态和实际结果；实现驱动后补精确入口，执行后补证据与失败原因，不以整个 V 包的单个通过标记覆盖未执行子项。

### 10.1 共用启动与证据门槛

当前已证明可用的范围是本机 Windows、项目 Rust 工具链、动态 loopback、本地 SQLite，以及现有 Local/Connections runner。隔离故障介质、可控远程目标、真实代理和 Unix 主机**未在本任务中指定并核验**，不等于认定用户没有这些环境；长期负载参数也尚未冻结。

| 门槛 | 执行前必须明确的输入 | 缺失时的处理 |
| --- | --- | --- |
| 目标与授权 | 主机/环境标识、执行账户、可丢弃工作目录绝对路径、允许连接的目标/端口、允许变更或终止的介质/进程；授权人及范围 | 不发送试探请求、不修改网络/权限、不发送信号；只开发不触及目标的本地夹具 |
| 版本与工具 | commit/源码指纹、配置指纹、Cargo.lock、实际工具版本/路径、构建模式、已有负载/采样工具 | 不自动安装代理、故障驱动或采样工具；工具缺口和审批单独列出 |
| 隔离与资源 | 测试数据是否可丢弃、磁盘/内存/连接硬上限、禁止触及的共享目标；负载时段、最大总时长 | 未冻结的参数保持“待确认”；不能把建议值当默认批准值 |
| 故障与清理 | 故障施加/解除步骤、操作 deadline、独立 watchdog、停止阈值、仅针对本次进程的清理步骤、恢复验证 | 无可验证恢复路径不得施加故障；watchdog kill 记录失败/清理，不算业务正常退出 |
| 证据与关闭 | 用例编号、实际命令、目标阶段到达证据、输入/期望/实际结果、错误分类、时序、恢复及终态 | 缺阶段证据为未验收，实际断言不符为失败；“不适用”必须说明支持范围依据，不能代替环境缺失 |

原始命令、地址、凭据引用、数据库和日志只进入获准环境的专用测试目录，本机统一在 `_fluxdns/` 下；共享文档仅保留脱敏配置摘要、目标别名和结果。每次运行须能对照同一版本的前置状态、故障期间和恢复后状态。环境表未填满只阻止相关实验，不阻止补本机确定性测试或编写带前置检查的驱动。

各门槛对应的组件和最小预检见[依赖清单](#11-测试开发依赖环境清单)，环境交付按[依赖交接记录](#119-依赖交接与预检结果)逐项留证；本节不把“测试已编译”视为外部依赖可用。

### 10.2 V3 剩余网络分支

**现状与缺口：** 已有在途 TCP read、TLS/响应头/body 取消与原 deadline、SOCKS 三阶段预算测试。尚无受控 OS connect 挂起的执行驱动，也没有跨 OS 的完整终态记录；body 断流测试只固定当前分类，不能当作分类设计已确认。

**环境门槛：** V3-R01 需要获准且可恢复的网络隔离范围，能只对测试目标丢弃建连响应，并以连接状态或等价证据确认实际 connect 已在途。记录目标别名、路由/规则影响范围、故障恢复方法和 watchdog；不得随机选择不可达公网 IP，也不得修改全机防火墙来制造挂起。V3-R02 需先明确要验收的 OS/版本集合及其可用采样能力，当前没有额外 OS 的通过证据。

| 编号 | 待执行步骤 | 通过条件与必须保留的证据 |
| --- | --- | --- |
| V3-R01 | 分别覆盖 Tokio 出站与 Reqwest HTTP 的适用 direct/连接 SOCKS 代理端点路径；确认实际 connect Pending 后，分别触发调用方取消与原 deadline 耗尽 | 返回原取消原因或 Timeout；记录开始、进入在途、取消/截止、返回时间及 task/socket 回收。预取消、预过期、成功拨号或 TLS 等待都不能替代；不要求强制抢占 OS 内部连接过程 |
| V3-R02 | 在明确列入验收的各 OS 上运行现有 TCP/TLS/HTTP/SOCKS suite 与 R01，追加对端 EOF/复位、请求取消、服务停机的终态观测 | 无成功伪报、悬挂任务或持续句柄增长；同时记录原始 OS 错误和 port 分类。TIME_WAIT 等内核状态与进程持有句柄分开统计，不要求所有内核记录立即消失 |
| V3-R03 | 保存 V3-H03 截断 body 的最小输入，对照 `map_reqwest_error` 与 DoH connector 的 `retryable` 映射，提交分类决策 | 当前为 `Internal`、不可重试；必须明确接受此残余限制，或另行确认应如何区分协议错误/读取故障及是否允许重试。调整后补分类和上层重试回归，不能只改断言使测试通过 |

R03 的下一步是契约决策，不依赖远程主机；R01/R02 的下一步是环境核验和显式驱动。任何一项未满足只保持该项开放，不抹掉已有本地 conformance 证据。

### 10.3 V5 真实介质故障

**现状与缺口：** 真实 SQLite 的 trigger、写锁和 SQL 同步点已测试；disk-full、权限拒绝、介质 I/O 失败均未实施驱动和运行验收。三者不能互相代替。

**环境门槛：** 提供独立可丢弃介质的标识、挂载/工作目录绝对路径、容量硬上限、实际文件系统、服务账户、剩余空间下限和已验证的恢复路径。确认主库、WAL、SHM 和临时文件实际所在介质。只有“仓库内有临时目录”不构成可填满介质授权；不能填满系统盘、修改个人目录权限或影响共享卷。权限测试须有确实受该权限约束的执行账户；I/O 测试须有现成且获准的故障设施，不能自行安装内核驱动。

| 编号 | 待执行步骤 | 通过条件与必须保留的证据 |
| --- | --- | --- |
| V5-R01 | Cache/Storage 分开创建合成基线，在隔离容量内制造真实 disk-full；分别命中启动建库/迁移/写探针与运行批写/提交，checkpoint 仅在适用路径单列 | 保存真实空间状态、OS/SQLite 返回码和失败阶段；未命中 WAL/checkpoint 的运行不得宣称该阶段已测。旧已提交内容不出现部分事务，Cache 降级与 Storage 失败/gap 如实报告 |
| V5-R02 | 分别在启动前、运行中施加已批准的文件/目录权限拒绝，命中打开、写入或文件创建等适用操作 | 证明服务身份下实际 I/O 被拒绝；权限命令成功但既有文件句柄仍可写时，该写入场景仍未验收。记录权限前后状态，解除后复核恢复 |
| V5-R03 | 用已获准设施在指定介质和阶段制造真实 I/O 失败，主库与 WAL 的可控故障点分别记录 | 具有介质/OS 与 SQLite 错误链，不能用 trigger、busy lock 或主动返回 `PortError` 代替；无法定向命中的阶段保持缺口 |
| V5-R04 | 对 R01–R03 分别验证 Cache best-effort 降级/恢复，以及 Storage 阈值前恢复和触及保护后的有限退出；恢复后重开数据库 | 对照已提交数据、ledger、pending/gap 和完整性结果，统计不重复入账；允许的 best-effort 丢弃有计数，不把 Storage 升级策略套到 Cache。分别报告进程内重试与重启结果，不承诺内存 pending 经重启无损重放；端口/句柄和故障设置恢复 |

每次施加故障前先做不注入故障的基线和恢复流程演练；只允许在批准介质内清理本次夹具。正式结果逐项列出“adapter × 故障类型 × 实际命中阶段 × 恢复/退出”，不能以一次写失败关闭整个 V5。

### 10.4 V6 连接恢复与真实代理

**现状与缺口：** V6-C01 已实现并运行 TCP/plain DoH 单次容量边界与槽位释放；尚未实现持续重连/reload、慢握手/body 混合的容量驱动及句柄趋势采样。真实 external 入站代理和真实出站 SOCKS 仍无环境记录，二者是不同链路。

**环境门槛：** 本机 R01/R02 可先扩展现有夹具，不要求先提供远程环境；运行前仍应冻结连接数量、轮数、持续时间、句柄观测和停止上限。R03 需要已存在且获准的 TLS 终止代理、证书/域名、后端绑定、信任 peer 范围及可控制的 forwarded/PROXY 发送方式。R04 需要获准 SOCKS5/SOCKS5H 端点、认证方式、代理侧 DNS 与目标 HTTP/HTTPS 服务，并能取得脱敏代理记录。不得为补测试临时安装反向代理；私钥、凭据和真实内部地址不入 Git。

| 编号 | 待执行步骤 | 通过条件与必须保留的证据 |
| --- | --- | --- |
| V6-R01 | 在 TCP/DoH 上连续执行占用 1,023/1,024/超额等待、释放、重连，交叉 listener 复用 reload、rebind 及 shutdown | 区分 TCP connect、内核 backlog、accepted session 与 active request；释放后恢复处理，失败 reload 保留旧 listener。每轮记录 task/guard/句柄，停机可重绑端口；不能用跨 listener 合计验证单 listener 限额 |
| V6-R02 | 复用 TLS/HTTP 夹具，把慢握手、慢 body、坏证书、截断/畸形 HTTP 与有效 GET/POST 混入容量/恢复周期 | 坏连接仅影响自身，受限阶段在既有预算内结束；有效请求的 HTTP 与 DNS 结果均正确。固定比例/轮数后检查有效流量与资源恢复，不能仅验证一次坏连接被拒绝 |
| V6-R03 | 通过真实 external TLS 终止链发送 GET/POST；覆盖可信/不可信 peer、forwarded 多跳、伪造头和适用 PROXY 版本/读取顺序 | 实际 TLS 终止位置、peer/头链及最终客户端身份可对照；不可信来源不能借头部提权。记录代理与 FluxDNS 两端结果；绕开代理直连 plain DoH 不算 external 通过 |
| V6-R04 | 用真实出站 SOCKS 分别运行 HTTP/HTTPS，核验本地解析的 SOCKS5、代理解析的 SOCKS5H 和显式 `connect_ip` 的支持组合；复用 V3 断言 | 代理记录实际 CONNECT 地址类型/目标，HTTP Host、TLS SNI/证书身份保持正确；认证失败、代理/目标不可达与取消不返回成功、不重置预算。仅 loopback 模拟代理或只连通端口不算真实代理环境证据 |

R01/R02 的驱动开发、R03/R04 的目标环境准备分别跟踪。只具备一种代理不能关闭另一种链路；只跑 Windows 不能关闭 V3-R02 的其他已列入验收平台。

### 10.5 V7 可控远程资源

**现状与缺口：** 已有 loopback 条件请求和本地 pair/代际测试；未实现真实远程编排驱动，未进行远程请求。本包验收资源拉取，不以远程 DoH 或代理连通性结果替代。

**环境门槛：** 提供已授权的端点别名、URL 的本地配置、可读写的合成资源范围、请求速率/次数限制和运行时段。端点须能控制 ETag/Last-Modified、200/304、内容版本、无效内容及延迟，并提供请求头/响应记录；确认 TLS 信任与代理路由。资源 GET 的认证要求必须适配当前 fetcher，不能假设支持任意认证头或 URL userinfo；控制端点的凭据与 FluxDNS 拉取资源的配置分开，详见 [E5](#116-e5-可控远程资源服务)。不能控制上述行为的任意公共 URL 只能做有限 smoke，不足以关闭本包。

| 编号 | 待执行步骤 | 通过条件与必须保留的证据 |
| --- | --- | --- |
| V7-R01 | 首次 200，使用服务器返回的 validator 请求 304，再切换内容并返回新 200；按端点能力分别验证 ETag/Last-Modified | 两端对照条件头、状态、版本/hash、matcher 与 content/manifest；304 不重造内容身份，新 200 正确更新，不能只记 HTTP 状态 |
| V7-R02 | 在专用本地副本中分别缺失/损坏 content 或 manifest，切换 URL 或 adapter generation，并让服务器重复返回 304/无效新内容 | 不误用旧 validator 或坏 pair；恢复请求沿用原 deadline；拒绝无效结果且不替换有效 snapshot。首次启动无可用内容与已有运行态失败分开断言 |
| V7-R03 | 在远程响应在途、304 后重试前分别取消；让远程刷新与配置 reload 交错 | 记录请求次数、取消/截止与发布顺序；取消不启动后续重试，旧候选不覆盖新代际；核对活动资源、matcher 和本地 pair，不仅检查远程返回成功 |

下一步先完成端点能力核验和不含凭据的编排驱动，再执行获准请求。服务器无请求记录、无法控制版本或没有确切命中交错点时，明确标为未验收而非“公网可用”。

### 10.6 V8 Unix 进程信号

**现状与缺口：** 源码有 Unix SIGTERM/SIGINT 和第二信号分支；当前没有真实 Unix binary 的信号编排驱动与验收记录。Windows Cancellation 单测和停止按钮不是替代证据。

**环境门槛：** 指定获准 Unix 主机/OS/内核、账户、专用绝对工作目录、binary 构建/来源指纹、独占测试端口及仅可终止的本次 PID 范围；确认已有信号发送和进程/句柄观察能力。需要可控在途上游或隔离 SQLite 等待，能证明 drain 已开始且仍未完成；没有该同步证据不能用固定 sleep 宣称第二信号命中。watchdog 必须独立于业务 grace，禁止宽泛终止同名进程。

| 编号 | 待执行步骤 | 通过条件与必须保留的证据 |
| --- | --- | --- |
| V8-R01 | 空闲与 DNS 在途状态分别发送一次 SIGTERM、一次 SIGINT；有剩余预算时再覆盖 late drain、详情批写、pending stats | 记录真实 PID、就绪/信号/退出时间及 shutdown 阶段；停止接收、取消在途，按现有顺序回收，成功退出码为 0；不要求每个已读请求均写回 |
| V8-R02 | 用受控等待确认首次信号已进入 drain 后，再发送 SIGTERM/SIGINT；分别记录四种首/第二信号组合 | 当前第二信号返回 `ServiceError::Signal`，Application 映射 `runtime_fatal`、退出码 5；必须证明第二次在 grace 内且进程仍在 drain。不能只把非零退出码当命中证据，也不要求强制完成 flush |
| V8-R03 | 只发送首信号，分别让整体 drain 或 Storage 的当前操作耗尽原预算 | 区分 `ShutdownDeadline -> shutdown_timeout` 与 Storage/Telemetry 阶段错误映射的 `runtime_fatal`，退出码均为 5；保留具体阶段和剩余工作。已执行 SQL 可能不可强制抢占，实测 elapsed 与限制必须报告 |
| V8-R04 | 对上述正常/失败退出逐次检查进程和监听端口，并重开专用数据库 | 无遗留测试子进程，端口可重新绑定，已提交数据及数据库完整性可核对；未落库的内存 pending/详情如实记录，不把错误退出描述为无损停机 |

退出码及分类依据为 [`Service`](../../backend/src/service.rs) 和 [`AppExitCode` / `map_service_error`](../../backend/src/app.rs) 的当前映射，不是新的退出策略。watchdog 强杀、默认信号终止或无法确认 drain 阶段的运行不得计作第二信号分支通过。

### 10.7 V9 持续积压与联合恢复

**现状与缺口：** 已有三轮 SQLite trigger 恢复和 pending 事件保护的局部正确性证据；持续流量、真实介质故障、详情容量与 Cache/连接/owner 的联合周期驱动尚未实现。连续运行现有单测不等于持续进程恢复。

**启动门槛：** 先补可重复的“稳态 → 增压/故障 → 解除 → 恢复/有限退出”编排；涉及真实介质的子项须有 V5 前置，涉及真实代理的子项须有 V6 对应前置。本机正常容量/owner 子集可独立开发。执行前逐场景冻结 QPS/并发、阶段时长、周期数、解除故障时点、允许恢复窗口和停止条件；当前均未形成获确认的运行参数。

| 编号 | 待执行步骤 | 通过条件与必须保留的证据 |
| --- | --- | --- |
| V9-R01 | 持续记录 stats/详情，跨事件 UTC 日桶并包含乱序/late event；让 pending 积压后在保护阈值前恢复，同时覆盖详情软/硬容量和年龄清理 | 以合成输入对照每日日桶、ledger 和详情集合，已提交批次不重复；pending/gap 在冻结窗口内恢复，清理顺序符合契约；记录仍在途/允许丢弃项，不能简单要求输入数等于详情行数 |
| V9-R02 | 分别触发 pending 批数与事件数保护，再执行失败升级和有限 flush/退出 | 当前源码上限为 64 批、65,536 pending events，运行前重新读源码并记录实际触发点；不通过调高生产上限延后失败。达到保护后的有限退出是独立通过路径，不要求进程无限服务或保证内存数据经重启恢复 |
| V9-R03 | Cache 持续插入/淘汰和持久化故障，交叉 namespace 变化、reload、故障解除与重开 | 核对编码预算、插入时间淘汰和恢复条目；旧 namespace/过期内容不被错误使用。主库/WAL/SHM 记录物理趋势，不把它们与编码预算作等值断言 |
| V9-R04 | 重复满载/释放/reload/资源刷新，加入 late candidate、队列拒收和 shutdown；分别使 SQL 空闲、在途及接近截止 | 每轮记录 session、request guard、lease、finalizer、队列/task/句柄基线与恢复值；无未解释持续增长或悬挂 waiter。停机共享原预算，stats 不能抢占已运行详情事务，剩余详情和失败阶段可解释 |

每轮必须能对照负载量、故障持续时间、积压峰值、恢复耗时及终态；V9-R02 的退出轨迹不能混入 V9-R01 的在线恢复成功率。未命中的阈值和联合条件仍开放，不能用三个 trigger 周期关闭。

### 10.8 V10 长期负载与性能验收

**现状与缺口：** 没有长期负载/性能对照驱动、目标环境曲线或已确认 SLO。现有 debug profile 和 Local/Connections 重复运行只证明其各自断言，不能推导长期稳定性。

以下运行输入必须逐项给出具体值及确认记录；当前均为**待确认**，第 7 节的建议时长不是执行授权：

| 待冻结项 | 必须填入的具体内容 |
| --- | --- |
| 构建与对照 | 候选/对照 commit、release features、lockfile、配置指纹、二进制指纹；若只做同机相对对照，明确不输出生产 SLO 结论 |
| 主机与负载器 | OS/CPU/内存/介质、网络拓扑、账户/进程/连接限制、负载器版本与资源占用、隔离时段；证明负载器未先饱和 |
| 流量参数 | UDP/TCP/DoH 比例、QPS 阶梯、并发/连接复用、查询集合与种子、命中/未命中/负答案比例、详情开关、上游延迟/故障比例 |
| 时间与恢复 | 预热/对照/持续运行时长、重复次数、采样间隔、故障/reload/刷新时刻和恢复周期；10 分钟/30 分钟/4 小时/至少 3 周期仅作为原计划评审起点 |
| 正确性与性能阈值 | 完成 QPS、成功/失败/超时口径，p50/p95/p99 及允许退化，统计去重/详情丢弃容限，故障解除后的恢复窗口 |
| 资源与停止阈值 | CPU/RSS 上限及增长趋势窗口，句柄/task/队列上限，磁盘余量下限，最大总时长/停机观察时间；每项越线的停止动作和清理负责人 |

| 编号 | 待执行步骤 | 通过条件与必须保留的证据 |
| --- | --- | --- |
| V10-R01 | 在固定输入和同一环境分别运行 Cache 命中、真实上游未命中、负答案、详情开/关与受控混合流量；对照/候选做多轮测量 | 报告请求总数、实际完成 QPS、成功/失败/超时的数量和耗时分布；端到端含不含负载器排队写清，主链耗时不混入异步落库等待 |
| V10-R02 | 按冻结时长持续混合流量，先单独再组合 reload、资源刷新、上游故障和数据库恢复 | 正确性复用 V9；持续记录 CPU/RSS、句柄/task/队列、gap/drop、物理磁盘及恢复曲线。完整时间轴无缺段，不从短时平稳片段推导长时通过 |
| V10-R03 | 按预定恢复窗口和资源/性能阈值评定每轮，再比较同条件候选与对照 | 阈值全部满足、无未解释持续增长；报告多轮波动和采样开销。越过安全阈值立即按计划停止，本轮失败/未完成，不换更宽阈值补记通过 |

没有业务 SLO 但已确认相对比较阈值时，只能给同机对照结论；未确认任何通过阈值时只能称探索性测量。若中途修改影响时序/性能的源码、构建或配置，重新冻结版本并重跑受影响场景，不拼接不同版本曲线。

## 11. 测试开发依赖环境清单

本节明确“为了开发、调试和执行第 10 节测试，需要先准备什么”。E0–E7 是本计划的依赖编号，不是新增生产配置、安装清单或已部署环境。软件版本与安装规则仍以[环境规范](../rules/environment-usage.md)、[`mise.toml`](../../mise.toml) 和 [`Cargo.toml`](../../backend/Cargo.toml) 为权威；这里仅列本任务实际用到的依赖及就绪条件。

| 依赖 | 编写/本地调试阶段 | 真实验收阶段 | 当前状态与阻塞范围 |
| --- | --- | --- | --- |
| E0 工具链与工作目录 | 所有测试/驱动编译及文档检查必需；只运行既有 binary 的远端不必安装开发工具链 | 构建侧必需，运行侧核对 binary 和运行库即可 | 本机曾完成 Local；新主机需重新预检，不把旧结果外推 |
| E1 本地 I/O 夹具 | V1–V4、V6-R01/R02 和 V9 本机子集必需；复用 Tokio、rcgen、sqlx | 提供本地正确性证据，不能代替以下外部环境 | 已有 loopback/SQLite 夹具可用，持续连接/联合恢复驱动仍需开发 |
| E2 受控网络故障域 | 驱动编排可先用 E1 调试，但 OS connect 挂起只能在 E2 调试到目标分支 | V3-R01；V3-R02 中相关 OS 的挂起分支必需 | 未指定/核验；不阻塞其他 read/TLS/body 本地测试和 V3-R03 决策 |
| E3 可丢弃故障介质 | V5 驱动先以正常临时库调试；真实权限/空间/I/O 故障需要 E3 | V5 及 V9/V10 中真实介质故障子项必需 | 三类故障设施分别未核验，不将其中一种就绪等同全部就绪 |
| E4 真实入站/出站代理 | 协议编排可用现有 loopback 代理/TLS 夹具调试 | V6-R03/R04，以及明确经过代理的 V7/V9/V10 子项必需 | external 与 SOCKS 两类均未指定/核验；不阻塞 V6-R01/R02 |
| E5 可控远程资源服务 | 200/304、版本/延迟编排可先用 E1 调试 | V7 和 V9/V10 中真实远程刷新子项必需 | 未指定/核验；远程主机存在但不能控制响应仍不就绪 |
| E6 Unix 进程环境 | Windows 可编写共用编排；Unix 信号分支的编译/调试需相应目标环境 | V8 及已列入跨 OS 验收的 Unix 子项必需 | 未指定/核验；不要求为此自动安装虚拟机、容器或子系统 |
| E7 负载与观测环境 | 低负载短周期可先调试采样、对账和停止逻辑 | V9 持续周期、V10 对照/长期运行必需 | 发生器/采样方案、目标资源与数值参数均待冻结 |

### 11.1 E0 工具链与工作目录

准备责任：开发者提供源码、构建命令和锁文件；目标主机维护者确认账户、已安装工具和目录权限。下列版本是当前仓库声明，不是要求升级机器上的其他项目。依实际命令选择所需工具；仅检查 Markdown 不要求构建 Rust，远端仅运行 binary 也不要求安装编译工具。

| 必需项 | 具体要求 | 最小就绪检查与失败处置 |
| --- | --- | --- |
| Git / mise / Rust | 现有 runner 直接调用 `git`、`mise`、`rustc`、`cargo`；Rust 使用项目 `1.98.0`，含 rustfmt，mise 满足 `mise.toml` 最低版本 | 检查实际可执行文件路径、项目解析版本及 `rustc -vV` 的 host；命令存在但解析到其他工具链不算就绪 |
| 原生编译与链接 | 目标 Rust ABI 所需的 C 编译器、linker 和系统开发库可用；当前 SQLite 依赖图启用 `libsqlite3-sys/bundled`，不是外接 SQLite 服务 | `cargo test --manifest-path backend/Cargo.toml --locked --no-run` 必须可编译/链接测试。缺 compiler/linker 是构建依赖缺口，不能靠安装 SQLite CLI 解决；补装须遵守安装规则 |
| PowerShell / .NET | 运行 `.ps1` runner 和文档检查器需要 PowerShell 7；runner 还使用 .NET 的 `ProcessStartInfo.ArgumentList`、进程树 Kill、`SHA256.HashData` 和 `Convert.ToHexString` | 记录 `pwsh` 与其 .NET 版本，并核对 API 可用；不能仅凭 major=7 就把较旧运行库视为兼容。Unix 未安装 pwsh 时可直接跑 Cargo，但不能声称执行了该 runner |
| Cargo 依赖可得性 | 已缓存本次 lockfile 依赖，或获准访问所需 registry/下载源；后端依赖缓存使用 `backend/.cargo-home` | 离线缺包与源码编译失败分开报告；不改锁文件或借其他全局依赖掩盖缺包，不将下载授权扩大为外部测试目标授权 |
| 目录与空间 | `backend/target`、项目依赖缓存、`_fluxdns/test-temp`、`_fluxdns/contract-validation` 可写；有容纳编译、数据库与日志的实际可用空间 | 验证本次账户能创建、写入、重命名和清理自己的小夹具；记录可用空间。不得把构建目录或报告目录放在待填满/故障化介质上 |
| 非必需组件 | 默认 features 的后端测试不需要 Node/pnpm、WebUI 构建、外部 PostgreSQL/MySQL/Redis 服务、SQLite CLI、OpenSSL 命令或反向代理 | rcgen/SQLx 已覆盖证书生成与数据库断言；只有选择 `webui-embed` 构建、人工 smoke 或额外诊断时，再按对应文档确认附加依赖 |

预检命令示例（从仓库根执行；本次文档修改未重跑这些构建命令）：

```powershell
Get-Command pwsh,mise,rustc,cargo,git -CommandType Application
mise ls rust --current
rustc -vV
cargo --version
cargo fmt --manifest-path backend/Cargo.toml -- --check
cargo test --manifest-path backend/Cargo.toml --locked --no-run
```

编译通过只关闭 E0 的构建门槛，不能证明 E1 I/O 或 E2–E7 环境可用。纯后端契约开发不应被完整发布 workflow 的前端依赖阻塞；安装/切换工具的权限不能从上述命令示例推定。

### 11.2 E1 本地 I/O 与可控夹具

准备责任：开发者维护合成输入、server/dialer、阶段同步和清理；主机维护者仅需在安全策略阻止本机 I/O 时确认允许范围。

| 必需项 | 能力与权限 | 最小就绪检查 |
| --- | --- | --- |
| 本地网络 | 允许普通测试进程绑定动态 loopback UDP/TCP 端口、建立本机连接；不占用生产 DNS/HTTPS 端口，不默认要求管理员权限 | 每种目标 transport 完成一次 bind → 请求/响应 → 关闭 → 重绑。只有 IPv4 可用时不能记录 IPv6 分支通过；端口/安全软件拦截单独报告 |
| 同步与 TLS | 复用 `TestGate`、Tokio socket/timer、rcgen、tokio-rustls；能独立控制握手/头/body 放行和取消，测试证书有预期 DNS 名称 | 握手成功/不受信/名称不符各命中预期分支；测试根只注入测试 client，不安装为系统根、不修改生产验证策略 |
| SQLite 文件系统 | 本次账户能创建主库及 WAL/SHM，执行事务、关闭、重开和删除自身夹具；TEMP/TMP/TMPDIR 指向本地测试根 | 以 SQLx 建库/写入/提交/重开/完整性检查，验证无其他进程占用；不能用 `:memory:` 代替需要文件/WAL 的用例 |
| 会话容量 | 测试客户端与服务端的 socket、任务、临时端口容量均足够；同机承载 1,025 条连接时需计入两端最多约 2,050 个连接 socket，另加 listener、连接池、数据库与观测余量 | 先记录进程句柄/FD 基线及有效软/硬限制，再显式运行 Connections；操作系统先拒绝建连时属于环境不足，不是命中 FluxDNS 的 1,024 accepted-session 边界 |
| 时间与证据 | 测试进程可读单调时钟，日志/报告可持续写入；socket/SQLite 使用实时钟 | 纯调度用例可暂停 Tokio 时间，真实 I/O 不依赖虚拟 timer；保存业务 deadline 和独立 watchdog，确认中途失败仍有报告 |

现有 Local/Connections 入口与输出说明见[后台服务](../implementation/backend/background-services.md#契约验证运行入口)。V6-R01/R02 的持续恢复驱动尚待实现，不能把 E1 就绪或一次 Connections 成功写成该驱动已完成。

### 11.3 E2 受控网络故障域

最小拓扑为“测试 client → 已授权且可恢复的故障点 → 合成目标或代理端点”。可以复用现有隔离环境，不要求新增某种网络产品；但必须提供下列能力，单纯 server 接受后不回 HTTP 只能制造读取等待，不能制造 TCP connect Pending。

| 必需依赖 | 需要交付的内容 | 就绪判据 |
| --- | --- | --- |
| 可选择的故障路径 | 精确到测试地址/端口的建连响应丢弃或等效隔离能力；能在恢复后连通同一目标 | 正常基线连通；获准启用故障后确实看到 connect 在途且未被立即拒绝/判定无路由；解除后再次连通 |
| 受限管理权限 | 故障规则/路由的操作对象、允许变更范围、原始状态、恢复命令及执行者 | 执行者能只影响本次测试路径；不接受全机断网、全局防火墙关闭或不明共享网络权限 |
| 阶段观察 | 已有连接状态、规则计数或获准抓取能力，以及 client 的开始/Pending/取消/返回时序 | 能区分 DNS 解析挂起、代理 TCP 建连挂起、SOCKS 协商挂起和目标 TLS 挂起；无实际阶段证据则依赖不就绪 |
| 双重清理 | 独立于测试 client 的 watchdog，能回收本次进程并恢复故障规则；报告目录在非故障路径 | 先演练恢复，不因 client 异常而遗留规则；被动观察能力不授予主动网络修改权限 |

网络维护者提供路径与规则权限，开发者提供分支命中判据和驱动；缺 E2 只阻塞真实挂起拨号及其跨 OS 复测，不能阻塞已有 read/TLS/body 用例。

### 11.4 E3 可丢弃故障介质

介质管理者提供隔离对象与恢复权限；开发者只在约定的数据库/合成文件范围操作。fault controller、watchdog 和证据日志必须位于故障域外，否则无法证明失效后的恢复和清理。

| 依赖 | 必需能力及输入 | 最小就绪检查与不足情形 |
| --- | --- | --- |
| 隔离容量 | 专用小容量文件系统/等价隔离介质，明确实际承载卷、总容量、测试占用上限、宿主剩余空间下限和回收方法 | 小规模基线确认主库/WAL/SHM 所在卷；批准后才能故障化。目录大小限制不自动等于介质 disk-full；配额拒绝与真实空间耗尽须按实际 OS/SQLite 结果分别归类 |
| 权限失败 | 可修改本次目录/文件权限的管理身份，以及会受该权限约束的测试服务身份；完整初始权限可恢复 | 管理身份和测试身份分开核验，不能以有绕过权限的服务身份测试。对已打开句柄和新建/重开文件分别验证实际拒绝；权限设置成功但仍可写即未命中 |
| I/O 故障设施 | 已存在且获准的介质/文件系统故障能力，明确是否能作用于 read/write/commit/checkpoint 及主库/WAL | 先在独立可丢弃小文件上证明真实 OS I/O 失败和恢复；不能以 SQLite trigger、忙锁或进程内错误 hook 替代。缺设施则 I/O 子项保持未就绪，不自行装驱动 |
| 数据与恢复工具 | 已提交基线、可重建合成输入、SQLx 查询/完整性检查；若选用 SQLite CLI，应先按工具规则确认它实际可用 | 同一账户正常建库、事务写入、关闭重开、内容核对通过；恢复后可撤销故障并清理本次文件，不使用个人/共享数据库做“备份后测试” |
| 空间与停止预算 | 故障卷上限、宿主安全余量、日志上限、数据库增长预算、最长故障时间和独立超时停止动作 | 每个数值执行前确认并留证，不以任意固定 GB 数假装通用最低配置；宿主安全余量无法监测时不得执行填充实验 |

disk-full、权限、I/O 三行必须分别登记就绪状态；有一个可写临时目录仅满足正常文件测试，不满足任何真实故障门槛。

### 11.5 E4 真实代理与 TLS 身份

代理维护者提供现有测试链路、配置变更/日志读取权限和端点信息；开发者提供脱敏场景与协议断言。以下两种代理分别准备，不能只填写一个“代理已提供”。

| 依赖 | external 入站 TLS 代理（V6-R03） | 出站 SOCKS（V6-R04） |
| --- | --- | --- |
| 网络链路 | 测试客户端能访问代理 TLS 端口，代理能访问 FluxDNS 内部 HTTP endpoint；后端不误当作 TLS endpoint | FluxDNS 能连接代理端点，代理能连接合成 HTTP/HTTPS 目标；为代理端点和目标分别说明 DNS/网络可达性 |
| 配置接入 | `tls.mode: external`，不带证书/私钥字段；`client_ip.source` 选择现有 `forwarded_header` 或 `proxy_protocol` 并限定 `trusted_proxies` | `outbound[].type: socks5`，`proxy_url` 用 env/file SecretRef，实际 scheme 为 `socks5://` 或 `socks5h://`；不是 HTTP CONNECT 代理替代品 |
| TLS / 名称 | 代理持有与测试域名匹配、测试客户端可信的证书链/私钥；记录 TLS 终止位置，私钥只留测试环境 | HTTPS 目标证书与原 URL 域名匹配，实际 FluxDNS client 信任该链；目标 IP、HTTP Host、TLS SNI 各自可观测，不能关闭证书验证换取连通 |
| 身份/解析对照 | 至少能产生可信和不可信两种实际 peer；多跳测试能给出实际头链，代理可清理/重写客户端伪造头 | SOCKS5 目标在 FluxDNS 本地解析，SOCKS5H 目标由代理解析；可取得 CONNECT 地址类型证据。`connect_ip` 保留 Host/SNI；SOCKS5H 不混配 bootstrap |
| 协议控制 | 转发 GET/POST 的 DNS body；验证 PROXY 时代理必须真能发送目标 v1/v2 头，使用单独配置的对应 endpoint | 提供支持的认证方式与测试凭据、拒绝认证场景、目标不可达场景；认证信息不进入共享输出 |
| 记录与初步连通 | 两端记录一次真实代理请求的 TLS 身份、HTTP/DNS 结果、peer/转发身份，再确认伪造输入被拒绝或按既定策略处理 | 先以实际 adapter 完成一次到合成目标的请求，对照代理 CONNECT 和目标收到的 Host/SNI；再核验每个待测解析/认证分支 |

TLS/身份字段的完整规则见[配置参考](../implementation/configuration.md#9-listener)。本地 `with_test_root_certificate` 只存在于测试构建，不是正式 binary 的 CA 配置入口；真实链路必须证明当前正式 client 可验证证书。客户端浏览器或 `curl` 信任证书，不自动证明 FluxDNS 也信任。

当前 Reqwest DoH 与资源 fetcher 都显式 `no_proxy()`，代理必须经现有配置接入，不能仅设置系统代理或 `HTTP_PROXY` 就声称走过测试代理。新增根信任、改代理配置或取得日志权限属于各自授权范围；不得为本包自行安装反向代理。

### 11.6 E5 可控远程资源服务

需要的不是“能返回文件的 URL”，而是能按用例改变状态并留下证据的测试服务。端点维护者提供控制方式和操作窗口，开发者提供确定的内容版本、请求顺序和恢复步骤。

| 依赖 | 必备能力 | 就绪检查 |
| --- | --- | --- |
| 内容与状态控制 | 两个可区分的有效内容版本、一个无效版本；可设置 ETag/Last-Modified，可强制返回 200/304，能恢复初始版本 | 不经 CDN/中间缓存的不可控改写，或有证据说明它们的影响；按一次 200 → 304 → 新 200 检查真实请求头、内容 hash 和 validator |
| 等待与取消同步 | 能暂停指定请求、确认服务器收到请求、独立解除暂停；可编排重复 304 和无效新内容 | 不能只靠估算公网延迟或固定 sleep 确认取消点；缺少暂停能力只满足非竞争子集，V7-R03 仍未就绪 |
| 访问方式兼容 | 资源 GET 使用当前 fetcher 支持的 HTTP/HTTPS URL，返回真实内容而非登录页；目标可通过获准网络/明确配置的 SOCKS 访问 | 当前代码拒绝 URL userinfo，不配置任意 `Authorization` 头，且不跟随重定向。只提供需网页登录、Bearer header 或跳转认证的端点，不属于已支持的可用环境 |
| 控制面凭据隔离 | 改版本/延迟的管理凭据只由外部编排使用；不冒充 FluxDNS 资源 GET 的认证配置 | 资源可通过隔离测试网络/获准访问控制等现有方式提供；需要新产品认证能力时先单独确认，不借验收增加配置键 |
| 双端证据 | 服务端请求记录可读，可对照状态/validator/版本；本地可读写专用 content/manifest 副本 | 一次实际 fetch 可关联到两端记录；不要求新增长期逐请求生产日志，可用测试端记录和已有本地状态断言 |
| TLS / 资源限制 | 正式 client 可验证目标证书和名称；请求速率、最大次数、正文上限、最长暂停时间、测试时段明确 | 正常拉取并解析合成内容成功后才进入异常矩阵；报告和本地 manifest 不泄漏实际凭据或敏感地址 |

访问方式依据 [`fetcher.rs`](../../backend/src/resource/fetcher.rs)；真实 HTTPS 不允许复用仅限 `cfg(test)` 的根证书注入来声称正式信任链通过。端点可部署在获准测试网络，不要求任意互联网公开访问，也不能未经授权对公共资源施加版本/延迟控制。

### 11.7 E6 Unix 进程环境

准备责任：主机维护者提供运行账户、OS/架构、专用目录及本次 PID 的管理权限；开发者提供 binary 来源、信号顺序与阶段观察。可用现有原生主机或已获准隔离环境，不把新安装容器、虚拟机或子系统作为默认前置。

| 依赖 | 必需能力 | 就绪检查 |
| --- | --- | --- |
| 真正 Unix 执行 | binary 的目标 OS/架构、运行库与宿主相容，实际执行 `cfg(unix)` 分支；若在目标机编译/跑 Rust tests 则另需 E0 | 核对内核/架构、binary 指纹与版本，能启动合成配置；仅在 Windows 交叉编译成功不算 Unix 运行证据 |
| 信号控制 | 有现成方式向本次精确 PID 发送 SIGTERM/SIGINT、等待退出并取得退出码；运行账户可控制自己创建的进程 | 先用可丢弃辅助进程确认发送对象、接收和退出码读取；不需要默认授予 root，也不使用宽泛同名进程 kill |
| 启动与 PID 身份 | 能读取真实服务 PID、启动时间/命令身份和 stdout/stderr；若经 wrapper/容器启动，信号转发关系必须清楚 | 发信号前再次校验 PID 身份，确认不是仅终止 wrapper 或让平台直接强杀；watchdog 与被测进程独立 |
| drain 阶段保持 | 可控上游或专用数据库等待能让真实服务进入并停留于 drain，同时能够观察阶段并放行 | 第二信号前可证明首信号已生效、服务仍在原 grace 内；普通请求在途不必然保证 drain 持续，必须实测同步点 |
| 退出后观察 | 能检查本次子进程、端口及文件句柄，重开专用 SQLite；临时目录位于该环境专用测试根 | 正常启动/退出的小规模预检后端口可重绑、数据库可读；缺 pwsh 不妨碍 Unix 信号本身，但共用 `.ps1` runner 需另行满足 E0 或实现等价报告入口 |

V8 的进程编排/报告入口仍待实现；安装了 Unix 或可以 SSH 登录仅证明部分访问能力，不能关闭信号控制、drain 同步及终态检查门槛。

### 11.8 E7 负载发生器与观测环境

准备责任：开发者提供流量/对账/停止编排，环境维护者提供资源隔离和采样权限，验收负责人确认数值阈值。可以复用已有工具或基于现有 Rust 依赖开发测试驱动，不预设必须安装某个压测平台。

| 依赖 | 必需能力 | 就绪检查及资源计算口径 |
| --- | --- | --- |
| 流量发生器 | 支持所测 UDP/TCP/DoH GET/POST、并发/连接复用、固定查询集合/种子、成功/失败/超时分计、延迟分布 | 先低负载核对 HTTP 与 DNS 结果，再用已知稳定目标核对设定 QPS 与实际发送/完成数；只支持 HTTP 的工具不能验收 UDP/TCP DNS |
| 非瓶颈发送侧 | CPU、内存、FD/socket、临时端口与网络容量能支撑冻结的流量；能观测发生器自身负载和排队 | 可分机，也可同机但必须证明发生器未先饱和。连接预算按同时打开的两端连接与池计算，不能只把服务上限 1,024 当整机 FD 需求 |
| 可控依赖服务 | 未命中/负答案上游、资源刷新、故障数据库都使用合成可控目标 | 涉及远程/代理/介质子项分别满足 E3/E4/E5；不把对公共 DNS 的长期压力当作默认依赖服务 |
| 持续采样 | 已有 API/工具能按冻结间隔获取服务和发生器 CPU/RSS、句柄/FD、连接、队列/owner、gap/drop、主库/WAL/SHM 大小及磁盘余量 | 原始时间序列有时间戳、单位和采样失败标记；某指标不可读时先补测试观察能力或明确缺口，不默认新增同步生产日志/高基数指标 |
| 容量与数据预算 | 明确日志每秒字节、采样记录规模、数据库增长估算、总运行时长、保留份数和安全余量 | 磁盘预算至少覆盖预估日志/采样/数据库增长及约定余量；内存/CPU 最低值由目标 QPS、并发和短时预检确定，不把旧机器配置写成通用最低硬件 |
| 对照隔离与时钟 | 候选/对照使用同一负载定义、构建条件和可比较环境；各端能关联事件时间，耗时用单调时钟 | 先核对短周期对账与采样时间轴，不通过改系统时钟制造 UTC 场景；记录其他进程负载和采样开销 |
| 停止与恢复 | 发生器可停止新增请求，独立 watchdog 可终止本次进程；资源/错误/时长越线时能执行预定收尾并保存记录 | 先低负载演练一次停止/恢复；没有确定 CPU/RSS/磁盘/错误阈值、最大时长或操作人，不进入持续运行 |

具体 QPS、轮数、持续时间和通过/停止值仍按 [V10 参数表](#108-v10-长期负载与性能验收)逐项冻结；本节的容量计算口径不代替实际数值确认。V9/V10 驱动待开发不是机器不足，采样权限不足也不是测试失败，三者须分别记录。

### 11.9 依赖交接与预检结果

环境准备按以下字段交接；这是本计划的记录要求，**不是 FluxDNS YAML schema，也不是已经实现的自动 preflight**。实际值存放于获准测试目录，共享文档只填脱敏摘要。

| 记录字段 | 必填内容 |
| --- | --- |
| 范围与负责人 | E 编号、关联 R 用例、环境提供/维护者、驱动开发者、验收确认人；允许的操作与时段 |
| 目标与接入 | OS/架构、账户、工作目录绝对路径、进程/卷/端点别名、允许端口/路径；凭据仅给 SecretRef 或本地引用，不给明文 |
| 已有组件 | 工具/服务版本及可执行文件位置、binary/配置指纹；组件安装来源与缺失项 |
| 网络与身份 | client/代理/目标的拓扑，实际 DNS 解析方、信任 peer、证书域名和信任验证结果 |
| 数值预算 | 并发/FD/CPU/RSS/空间、请求速率/次数、故障/总时长、采样间隔、停止阈值及恢复窗口 |
| 预检证据 | 实际命令、执行时间、退出码、最小请求/事务/信号结果、日志位置；每个能力单列通过/失败/未执行 |
| 清理与状态 | 初始状态、恢复步骤、操作人及恢复验证；标明“可开发/可本地调试/可真实验收”，列出仍阻塞的具体 R 编号 |

开发者在相关真实测试开工前复核依赖交接，不仅检查“能登录”或“端口开着”。任何必需能力缺失时，不得自动换成 mock 后仍记录真实验收通过；记录缺口和最小补齐动作，继续不依赖该环境的开发工作。
